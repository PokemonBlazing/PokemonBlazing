/* const Pokedex = require("pokedex-promise-v2");
const P = new Pokedex()

function getRandomInt(max) {
    return Math.floor(Math.random() * max);
  }

function generarPokemon()
{
P.getPokemonByName(getRandomInt(151), (res,err)=>{
    if(!err){
        console.log(res);
    } else{
        console.log(err);
    }
})
}
const bulbasaur = {
        id: 1,
        name: bulbasaur,
        type: plant,
        attaks:
            {
                placaje: {
                    potencia: 40,
                    PP: 35,
                },
                latigo_cepa:{
                    potencia: 45,
                    PP: 25,
                },        
            },
        deffense: 20,
        
}
module.exports = 
{generarPokemon} */